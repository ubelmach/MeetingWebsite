import { Info } from 'src/app/models/Info';

export class InfoFromDb{
    constructor(
        purposes: Info[],
        languages: Info[],
        badHabits: Info[],
        interests: Info[],
    ) {}
}